@model WebApp.Areas.Admin.Models.UserRoles.CreateViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>AppUser</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="User.Id" class="control-label">User</label>
                <select asp-for="User.Id" class ="form-control" asp-items="Model.Users"></select>
            </div>
            <div class="form-group">
                <label asp-for="Role.Id" class="control-label">Role</label>
                <select asp-for="Role.Id" class ="form-control" asp-items="Model.Roles"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
