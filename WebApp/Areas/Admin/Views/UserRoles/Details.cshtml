@model WebApp.Areas.Admin.Models.UserRoles.DetailsViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>AppUser</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.Raw("User")
        </dt>
        <dd class = "col-sm-10">
            @Html.Raw(Model.User.Email)
        </dd>
        <dt class = "col-sm-2">
            @Html.Raw("Role")
        </dt>
        <dd class = "col-sm-10">
            @Html.Raw(Model.Role.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
