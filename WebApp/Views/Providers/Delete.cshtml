@model WebApp.BLL.DTO.Provider

@{
    ViewData["Title"] = WebApp.Resources.WebApp.Views.Providers.Delete.Title;
}

<h1>@WebApp.Resources.WebApp.Views.Providers.Delete.Title</h1>

<h3>@WebApp.Resources.WebApp.Views.Providers.Delete.AreYouSure</h3>
<div>
    <h4>@WebApp.Resources.WebApp.Views.Providers.Delete.Provider</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @WebApp.Resources.WebApp.Views.Providers.Delete.Name
        </dt>
        <dd class = "col-sm-10">
            @Model.Name
        </dd>
        <dt class = "col-sm-2">
            @WebApp.Resources.WebApp.Views.Providers.Delete.Price
        </dt>
        <dd class = "col-sm-10">
            @Model.Price.ToString("0.00")
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="@WebApp.Resources.WebApp.Views.Providers.Delete.Title" class="btn btn-danger" /> |
        <a asp-action="Index">@WebApp.Resources.WebApp.Views.Providers.Delete.BackToList</a>
    </form>
</div>
