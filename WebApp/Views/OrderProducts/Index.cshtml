@model IEnumerable<WebApp.BLL.DTO.OrderProduct>

@{
    ViewData["Title"] = WebApp.Resources.WebApp.Views.OrderProducts.Index.Title;
}

<h1>@WebApp.Resources.WebApp.Views.OrderProducts.Index.Title</h1>

<p>
    <a asp-action="Create">@WebApp.Resources.WebApp.Views.OrderProducts.Index.Create</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @WebApp.Resources.WebApp.Views.OrderProducts.Index.Order
            </th>
            <th>
                @WebApp.Resources.WebApp.Views.OrderProducts.Index.Product
            </th>
            <th>
                @WebApp.Resources.WebApp.Views.OrderProducts.Index.Quantity
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order!.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product!.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">@WebApp.Resources.WebApp.Views.OrderProducts.Index.Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">@WebApp.Resources.WebApp.Views.OrderProducts.Index.Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">@WebApp.Resources.WebApp.Views.OrderProducts.Index.Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
